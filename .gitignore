node_modules/
dist/
.env
.logs/
.DS_Store
__pycache__/

# What the relay learns about the folder it was started in — corrections, turn
# records, edited prompts. There is one of these per project; this is the repo's own.
.duck-talk/

# iOS app — .xcodeproj and both Info.plists are generated from app/project.yml
app/*.xcodeproj
app/.build/
app/.venv/
app/.team
app/DuckTalk/Resources/Info.plist
app/DuckTalkWidget/Info.plist
xcuserdata/
# SourceKit-LSP build context — absolute paths, one per machine; `./dt gen` writes it
app/buildServer.json
.probe/
